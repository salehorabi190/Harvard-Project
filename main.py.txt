<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Zakyah Symbolic Platform</title>
  <style>
    body { font-family: 'Segoe UI', sans-serif; background: #f4f6fb; margin: 0; padding: 20px; }
    header { background: #2c3e50; color: white; padding: 20px; text-align: center; }
    section { background: white; margin: 20px 0; padding: 20px; border-radius: 8px; box-shadow: 0 2px 6px rgba(0,0,0,0.1); }
    h2 { color: #34495e; }
    .formula, .output { font-family: 'Courier New', monospace; background: #ecf0f1; padding: 10px; border-radius: 5px; margin-top: 10px; }
    label { display: block; margin: 8px 0; }
    input[type="number"], input[type="text"], input[type="color"] { width: 120px; margin-left: 10px; }
    button { margin-top: 10px; padding: 8px 16px; background: #2980b9; color: white; border: none; border-radius: 4px; cursor: pointer; }
    button:hover { background: #3498db; }
    table { width: 100%; border-collapse: collapse; margin-top: 10px; }
    th, td { border: 1px solid #ccc; padding: 8px; text-align: center; }
    th { background-color: #2c3e50; color: white; }
  </style>
</head>
<body>

<header>
  <h1>Zakyah Symbolic Platform</h1>
  <p>Institutional Evaluation through Purification, Mission, and Harmony</p>
  <h3 style="color:#8e44ad;">Founded by Dr. Saleh — Creator of the Spiritual Economy Theory</h3>
</header>

<!-- تخصيص الهوية الرمزية -->
<section>
  <h2>🎨 Customize Symbolic Identity</h2>
  <form id="identityForm">
    <label>Institution Name: <input type="text" name="name" value="Zakyah Institute"></label>
    <label>Color for Purification (Zᵣ): <input type="color" name="colorZ" value="#9b59b6"></label>
    <label>Color for Mission (Mᵣ): <input type="color" name="colorM" value="#f1c40f"></label>
    <label>Color for Harmony (Nᵣ): <input type="color" name="colorN" value="#3498db"></label>
    <label>Symbol for Purification: <input type="text" name="symbolZ" value="✨"></label>
    <label>Symbol for Mission: <input type="text" name="symbolM" value="📜"></label>
    <label>Symbol for Harmony: <input type="text" name="symbolN" value="🌀"></label>
    <button type="button" onclick="applyIdentity()">Apply Identity</button>
  </form>
  <div id="identityPreview"></div>
</section>

<!-- لوحة التحكم الرمزية -->
<section>
  <h2>📊 Symbolic Dashboard</h2>
  <table>
    <thead>
      <tr>
        <th>Index</th>
        <th>Score</th>
        <th>Dimension</th>
        <th>Symbolic Classification</th>
      </tr>
    </thead>
    <tbody id="dashboardTable">
      <tr><td>ZWPI</td><td>72</td><td>Work Performance</td><td>🟡 Functionally Symbolic</td></tr>
      <tr><td>ZLI</td><td>85</td><td>Leadership</td><td>🟣 Symbolically Purified</td></tr>
      <tr><td>ZGI</td><td>60</td><td>Governance</td><td>🟡 Functionally Symbolic</td></tr>
      <tr><td>ZII</td><td>48</td><td>Investment</td><td>⚪ Purely Material</td></tr>
      <tr><td>ZIQI</td><td>78</td><td>Institutional Quality</td><td>🟡 Functionally Symbolic</td></tr>
      <tr><td>ZEVI</td><td>90</td><td>Existential Verification</td><td>🟣 Symbolically Purified</td></tr>
    </tbody>
  </table>
</section>

<!-- الرادار الرمزي -->
<section>
  <h2>🌀 Symbolic Radar Chart</h2>
  <canvas id="symbolicRadar" width="400" height="400"></canvas>
  <button onclick="renderRadar()">Render Radar</button>
</section>

<!-- التوصيات التطويرية -->
<section>
  <h2>🎯 Development Recommendations</h2>
  <button onclick="generateRecommendations()">Generate Recommendations</button>
  <div class="output" id="recommendationBox">Recommendations will appear here...</div>
</section>

<script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
<script>
  function applyIdentity() {
    const form = document.forms['identityForm'];
    const name = form.name.value;
    const colorZ = form.colorZ.value;
    const colorM = form.colorM.value;
    const colorN = form.colorN.value;
    const symbolZ = form.symbolZ.value;
    const symbolM = form.symbolM.value;
    const symbolN = form.symbolN.value;

    const preview = `
      <h3>${name}</h3>
      <p><span style="color:${colorZ}; font-size:20px;">${symbolZ}</span> Purification</p>
      <p><span style="color:${colorM}; font-size:20px;">${symbolM}</span> Mission</p>
      <p><span style="color:${colorN}; font-size:20px;">${symbolN}</span> Harmony</p>
    `;
    document.getElementById('identityPreview').innerHTML = preview;
  }

  function renderRadar() {
    const ctx = document.getElementById('symbolicRadar').getContext('2d');
    new Chart(ctx, {
      type: 'radar',
      data: {
        labels: ["ZWPI", "ZLI", "ZGI", "ZII", "ZIQI", "ZEVI"],
        datasets: [{
          label: "Symbolic Indices",
          data: [72, 85, 60, 48, 78, 90],
          backgroundColor: "rgba(155, 89, 182, 0.2)",
          borderColor: "#8e44ad",
          pointBackgroundColor: "#f1c40f"
        }]
      },
      options: {
        scales: { r: { beginAtZero: true, max: 100 } },
        plugins: { legend: { labels: { color: "#2c3e50" } } }
      }
    });
  }

  function generateRecommendations() {
    let output = "";
    const tazkiyah = (78 + 90) / 2;
    const message = (72 + 48 + 85) / 3;
    const harmony = (60 + 72) / 2;

    if (tazkiyah < 50) output += "🔮 تزكية منخفضة: نفّذ ورش تزكية جماعية، راجع النية المؤسسية.<br>";
    if (message < 50) output += "📜 رسالة ضعيفة: أعد صياغة الرسالة، صمّم استثمارات رسالية.<br>";
    if (harmony < 50) output += "🌀 انسجام متذبذب: حلّل الخطاب الداخلي، عزّز الانتماء.<br>";
    if (output === "") output = "✅ المؤشرات الرمزية متزكية، لا توجد توصيات حرجة حالياً.";

    document.getElementById("recommendationBox").innerHTML = output;
  }
</script>

</body>
</html>

ـــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــــ
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Zakyah Symbolic Platform</title>
  <style>
    body { font-family: 'Segoe UI', sans-serif; background: #f4f6fb; margin: 0; padding: 20px; }
    header { background: #2c3e50; color: white; padding: 20px; text-align: center; }
    section { background: white; margin: 20px 0; padding: 20px; border-radius: 8px; box-shadow: 0 2px 6px rgba(0,0,0,0.1); }
    h2 { color: #34495e; }
    .formula, .output { font-family: 'Courier New', monospace; background: #ecf0f1; padding: 10px; border-radius: 5px; margin-top: 10px; }
    label { display: block; margin: 8px 0; }
    input[type="number"], input[type="text"] { width: 100px; margin-left: 10px; }
    button { margin-top: 10px; padding: 8px 16px; background: #2980b9; color: white; border: none; border-radius: 4px; cursor: pointer; }
    button:hover { background: #3498db; }
  </style>
</head>
<body>

<header>
  <h1>Zakyah Symbolic Platform</h1>
  <p>Symbolic Evaluation & Development System</p>
</header>

<section>
  <h2>🔧 ZWPI Equation Editor</h2>
  <form id="equationForm">
    <label>α (alpha): <input type="number" step="0.01" name="alpha" value="0.0"></label>
    <label>β₁ (Rᵣ): <input type="number" step="0.01" name="b1" value="1.0"></label>
    <label>β₂ (Mᵣ): <input type="number" step="0.01" name="b2" value="1.0"></label>
    <label>β₃ (Tᵣ): <input type="number" step="0.01" name="b3" value="1.0"></label>
    <label>β₄ (Cᵣ): <input type="number" step="0.01" name="b4" value="1.0"></label>
    <button type="button" onclick="updateEquation()">Update Equation</button>
  </form>
  <div class="formula" id="equationDisplay">Pᵣ = α + β₁·Rᵣ + β₂·Mᵣ + β₃·Tᵣ + β₄·Cᵣ + εᵣ</div>
</section>

<section>
  <h2>📝 Symbolic Survey Input</h2>
  <form id="surveyForm">
    <label>Rᵣ (Message Impact): <input type="number" step="0.01" name="r"></label>
    <label>Mᵣ (Mission Alignment): <input type="number" step="0.01" name="m"></label>
    <label>Tᵣ (Transcendence): <input type="number" step="0.01" name="t"></label>
    <label>Cᵣ (Contribution): <input type="number" step="0.01" name="c"></label>
    <button type="button" onclick="calculateZWPI()">Calculate ZWPI</button>
  </form>
  <div class="output" id="zwpiResult">ZWPI Score: —</div>
</section>

<script>
  let equationParams = {
    alpha: 0.0,
    b1: 1.0,
    b2: 1.0,
    b3: 1.0,
    b4: 1.0
  };

  function updateEquation() {
    const form = document.forms['equationForm'];
    equationParams.alpha = parseFloat(form.alpha.value);
    equationParams.b1 = parseFloat(form.b1.value);
    equationParams.b2 = parseFloat(form.b2.value);
    equationParams.b3 = parseFloat(form.b3.value);
    equationParams.b4 = parseFloat(form.b4.value);

    const eq = `Pᵣ = ${equationParams.alpha} + ${equationParams.b1}·Rᵣ + ${equationParams.b2}·Mᵣ + ${equationParams.b3}·Tᵣ + ${equationParams.b4}·Cᵣ + εᵣ`;
    document.getElementById('equationDisplay').textContent = eq;
  }

  function calculateZWPI() {
    const form = document.forms['surveyForm'];
    const r = parseFloat(form.r.value);
    const m = parseFloat(form.m.value);
    const t = parseFloat(form.t.value);
    const c = parseFloat(form.c.value);

    const score = equationParams.alpha + equationParams.b1 * r + equationParams.b2 * m + equationParams.b3 * t + equationParams.b4 * c;
    document.getElementById('zwpiResult').textContent = `ZWPI Score: ${score.toFixed(2)}`;
  }
</script>

</body>
</html>

<section>
  <h2>🔧 ZLI Equation Editor</h2>
  <form id="zliForm">
    <label>α (alpha): <input type="number" step="0.01" name="alpha" value="0.0"></label>
    <label>β₁ (Zᵣ - Purification): <input type="number" step="0.01" name="b1" value="1.0"></label>
    <label>β₂ (Mᵣ - Mission): <input type="number" step="0.01" name="b2" value="1.0"></label>
    <label>β₃ (Iᵣ - Inspiration): <input type="number" step="0.01" name="b3" value="1.0"></label>
    <label>β₄ (Cᵣ - Contribution): <input type="number" step="0.01" name="b4" value="1.0"></label>
    <label>β₅ (Vᵣ - Verification): <input type="number" step="0.01" name="b5" value="1.0"></label>
    <label>β₆ (Rᵣ - Resonance): <input type="number" step="0.01" name="b6" value="1.0"></label>
    <button type="button" onclick="updateZLIEquation()">Update Equation</button>
  </form>
  <div class="formula" id="zliEquationDisplay">Eᵣ = α + β₁·Zᵣ + β₂·Mᵣ + β₃·Iᵣ + β₄·Cᵣ + β₅·Vᵣ + β₆·Rᵣ + εᵣ</div>
</section>

<section>
  <h2>📝 ZLI Survey Input</h2>
  <form id="zliSurvey">
    <label>Zᵣ (Purification): <input type="number" step="0.01" name="z"></label>
    <label>Mᵣ (Mission): <input type="number" step="0.01" name="m"></label>
    <label>Iᵣ (Inspiration): <input type="number" step="0.01" name="i"></label>
    <label>Cᵣ (Contribution): <input type="number" step="0.01" name="c"></label>
    <label>Vᵣ (Verification): <input type="number" step="0.01" name="v"></label>
    <label>Rᵣ (Resonance): <input type="number" step="0.01" name="r"></label>
    <button type="button" onclick="calculateZLI()">Calculate ZLI</button>
  </form>
  <div class="output" id="zliResult">ZLI Score: —</div>
</section>

<script>
  let zliParams = {
    alpha: 0.0,
    b1: 1.0,
    b2: 1.0,
    b3: 1.0,
    b4: 1.0,
    b5: 1.0,
    b6: 1.0
  };

  function updateZLIEquation() {
    const form = document.forms['zliForm'];
    zliParams.alpha = parseFloat(form.alpha.value);
    zliParams.b1 = parseFloat(form.b1.value);
    zliParams.b2 = parseFloat(form.b2.value);
    zliParams.b3 = parseFloat(form.b3.value);
    zliParams.b4 = parseFloat(form.b4.value);
    zliParams.b5 = parseFloat(form.b5.value);
    zliParams.b6 = parseFloat(form.b6.value);

    const eq = `Eᵣ = ${zliParams.alpha} + ${zliParams.b1}·Zᵣ + ${zliParams.b2}·Mᵣ + ${zliParams.b3}·Iᵣ + ${zliParams.b4}·Cᵣ + ${zliParams.b5}·Vᵣ + ${zliParams.b6}·Rᵣ + εᵣ`;
    document.getElementById('zliEquationDisplay').textContent = eq;
  }

  function calculateZLI() {
    const form = document.forms['zliSurvey'];
    const z = parseFloat(form.z.value);
    const m = parseFloat(form.m.value);
    const i = parseFloat(form.i.value);
    const c = parseFloat(form.c.value);
    const v = parseFloat(form.v.value);
    const r = parseFloat(form.r.value);

    const score = zliParams.alpha + zliParams.b1 * z + zliParams.b2 * m + zliParams.b3 * i + zliParams.b4 * c + zliParams.b5 * v + zliParams.b6 * r;
    document.getElementById('zliResult').textContent = `ZLI Score: ${score.toFixed(2)}`;
  }
</script>

<section>
  <h2>🔧 ZGI Equation Editor</h2>
  <form id="zgiForm">
    <label>α (alpha): <input type="number" step="0.01" name="alpha" value="0.0"></label>
    <label>β₁ (Nᵣ - Intent): <input type="number" step="0.01" name="b1" value="1.0"></label>
    <label>β₂ (Tᵣ - Transparency): <input type="number" step="0.01" name="b2" value="1.0"></label>
    <label>β₃ (Mᵣ - Mission): <input type="number" step="0.01" name="b3" value="1.0"></label>
    <label>β₄ (Aᵣ - Alignment): <input type="number" step="0.01" name="b4" value="1.0"></label>
    <button type="button" onclick="updateZGIEquation()">Update Equation</button>
  </form>
  <div class="formula" id="zgiEquationDisplay">Gᵣ = α + β₁·Nᵣ + β₂·Tᵣ + β₃·Mᵣ + β₄·Aᵣ + εᵣ</div>
</section>

<section>
  <h2>📝 ZGI Survey Input</h2>
  <form id="zgiSurvey">
    <label>Nᵣ (Intent): <input type="number" step="0.01" name="n"></label>
    <label>Tᵣ (Transparency): <input type="number" step="0.01" name="t"></label>
    <label>Mᵣ (Mission): <input type="number" step="0.01" name="m"></label>
    <label>Aᵣ (Alignment): <input type="number" step="0.01" name="a"></label>
    <button type="button" onclick="calculateZGI()">Calculate ZGI</button>
  </form>
  <div class="output" id="zgiResult">ZGI Score: —</div>
</section>

<script>
  let zgiParams = {
    alpha: 0.0,
    b1: 1.0,
    b2: 1.0,
    b3: 1.0,
    b4: 1.0
  };

  function updateZGIEquation() {
    const form = document.forms['zgiForm'];
    zgiParams.alpha = parseFloat(form.alpha.value);
    zgiParams.b1 = parseFloat(form.b1.value);
    zgiParams.b2 = parseFloat(form.b2.value);
    zgiParams.b3 = parseFloat(form.b3.value);
    zgiParams.b4 = parseFloat(form.b4.value);

    const eq = `Gᵣ = ${zgiParams.alpha} + ${zgiParams.b1}·Nᵣ + ${zgiParams.b2}·Tᵣ + ${zgiParams.b3}·Mᵣ + ${zgiParams.b4}·Aᵣ + εᵣ`;
    document.getElementById('zgiEquationDisplay').textContent = eq;
  }

  function calculateZGI() {
    const form = document.forms['zgiSurvey'];
    const n = parseFloat(form.n.value);
    const t = parseFloat(form.t.value);
    const m = parseFloat(form.m.value);
    const a = parseFloat(form.a.value);

    const score = zgiParams.alpha + zgiParams.b1 * n + zgiParams.b2 * t + zgiParams.b3 * m + zgiParams.b4 * a;
    document.getElementById('zgiResult').textContent = `ZGI Score: ${score.toFixed(2)}`;
  }
</script>

<section>
  <h2>🔧 ZII Equation Editor</h2>
  <form id="ziiForm">
    <label>α (alpha): <input type="number" step="0.01" name="alpha" value="0.0"></label>
    <label>β₁ (Nᵣ - Intent): <input type="number" step="0.01" name="b1" value="1.0"></label>
    <label>β₂ (Zᵣ - Purification): <input type="number" step="0.01" name="b2" value="1.0"></label>
    <label>β₃ (Sᵣ - Sustainability): <input type="number" step="0.01" name="b3" value="1.0"></label>
    <label>β₄ (Eᵣ - Leadership): <input type="number" step="0.01" name="b4" value="1.0"></label>
    <button type="button" onclick="updateZIIEquation()">Update Equation</button>
  </form>
  <div class="formula" id="ziiEquationDisplay">Rᵣ = α + β₁·Nᵣ + β₂·Zᵣ + β₃·Sᵣ + β₄·Eᵣ + εᵣ</div>
</section>

<section>
  <h2>📝 ZII Survey Input</h2>
  <form id="ziiSurvey">
    <label>Nᵣ (Intent): <input type="number" step="0.01" name="n"></label>
    <label>Zᵣ (Purification): <input type="number" step="0.01" name="z"></label>
    <label>Sᵣ (Sustainability): <input type="number" step="0.01" name="s"></label>
    <label>Eᵣ (Leadership): <input type="number" step="0.01" name="e"></label>
    <button type="button" onclick="calculateZII()">Calculate ZII</button>
  </form>
  <div class="output" id="ziiResult">ZII Score: —</div>
</section>

<script>
  let ziiParams = {
    alpha: 0.0,
    b1: 1.0,
    b2: 1.0,
    b3: 1.0,
    b4: 1.0
  };

  function updateZIIEquation() {
    const form = document.forms['ziiForm'];
    ziiParams.alpha = parseFloat(form.alpha.value);
    ziiParams.b1 = parseFloat(form.b1.value);
    ziiParams.b2 = parseFloat(form.b2.value);
    ziiParams.b3 = parseFloat(form.b3.value);
    ziiParams.b4 = parseFloat(form.b4.value);

    const eq = `Rᵣ = ${ziiParams.alpha} + ${ziiParams.b1}·Nᵣ + ${ziiParams.b2}·Zᵣ + ${ziiParams.b3}·Sᵣ + ${ziiParams.b4}·Eᵣ + εᵣ`;
    document.getElementById('ziiEquationDisplay').textContent = eq;
  }

  function calculateZII() {
    const form = document.forms['ziiSurvey'];
    const n = parseFloat(form.n.value);
    const z = parseFloat(form.z.value);
    const s = parseFloat(form.s.value);
    const e = parseFloat(form.e.value);

    const score = ziiParams.alpha + ziiParams.b1 * n + ziiParams.b2 * z + ziiParams.b3 * s + ziiParams.b4 * e;
    document.getElementById('ziiResult').textContent = `ZII Score: ${score.toFixed(2)}`;
  }
</script>

<section>
  <h2>🔧 ZIQI Equation Editor</h2>
  <form id="ziqiForm">
    <label>α (alpha): <input type="number" step="0.01" name="alpha" value="0.0"></label>
    <label>β₁ (Nᵣ - Intent): <input type="number" step="0.01" name="b1" value="1.0"></label>
    <label>β₂ (Zᵣ - Purification): <input type="number" step="0.01" name="b2" value="1.0"></label>
    <label>β₃ (Mᵣ - Mission): <input type="number" step="0.01" name="b3" value="1.0"></label>
    <label>β₄ (Cᵣ - Contribution): <input type="number" step="0.01" name="b4" value="1.0"></label>
    <label>β₅ (Fᵣ - Fulfillment): <input type="number" step="0.01" name="b5" value="1.0"></label>
    <button type="button" onclick="updateZIQIEquation()">Update Equation</button>
  </form>
  <div class="formula" id="ziqiEquationDisplay">Qᵣ = α + β₁·Nᵣ + β₂·Zᵣ + β₃·Mᵣ + β₄·Cᵣ + β₅·Fᵣ + εᵣ</div>
</section>

<section>
  <h2>📝 ZIQI Survey Input</h2>
  <form id="ziqiSurvey">
    <label>Nᵣ (Intent): <input type="number" step="0.01" name="n"></label>
    <label>Zᵣ (Purification): <input type="number" step="0.01" name="z"></label>
    <label>Mᵣ (Mission): <input type="number" step="0.01" name="m"></label>
    <label>Cᵣ (Contribution): <input type="number" step="0.01" name="c"></label>
    <label>Fᵣ (Fulfillment): <input type="number" step="0.01" name="f"></label>
    <button type="button" onclick="calculateZIQI()">Calculate ZIQI</button>
  </form>
  <div class="output" id="ziqiResult">ZIQI Score: —</div>
</section>

<script>
  let ziqiParams = {
    alpha: 0.0,
    b1: 1.0,
    b2: 1.0,
    b3: 1.0,
    b4: 1.0,
    b5: 1.0
  };

  function updateZIQIEquation() {
    const form = document.forms['ziqiForm'];
    ziqiParams.alpha = parseFloat(form.alpha.value);
    ziqiParams.b1 = parseFloat(form.b1.value);
    ziqiParams.b2 = parseFloat(form.b2.value);
    ziqiParams.b3 = parseFloat(form.b3.value);
    ziqiParams.b4 = parseFloat(form.b4.value);
    ziqiParams.b5 = parseFloat(form.b5.value);

    const eq = `Qᵣ = ${ziqiParams.alpha} + ${ziqiParams.b1}·Nᵣ + ${ziqiParams.b2}·Zᵣ + ${ziqiParams.b3}·Mᵣ + ${ziqiParams.b4}·Cᵣ + ${ziqiParams.b5}·Fᵣ + εᵣ`;
    document.getElementById('ziqiEquationDisplay').textContent = eq;
  }

  function calculateZIQI() {
    const form = document.forms['ziqiSurvey'];
    const n = parseFloat(form.n.value);
    const z = parseFloat(form.z.value);
    const m = parseFloat(form.m.value);
    const c = parseFloat(form.c.value);
    const f = parseFloat(form.f.value);

    const score = ziqiParams.alpha + ziqiParams.b1 * n + ziqiParams.b2 * z + ziqiParams.b3 * m + ziqiParams.b4 * c + ziqiParams.b5 * f;
    document.getElementById('ziqiResult').textContent = `ZIQI Score: ${score.toFixed(2)}`;
  }
</script>

<section>
  <h2>🔧 ZEVI Equation Editor</h2>
  <form id="zeviForm">
    <label>α (alpha): <input type="number" step="0.01" name="alpha" value="0.0"></label>
    <label>β₁ (Mᵣ - Mission): <input type="number" step="0.01" name="b1" value="1.0"></label>
    <label>β₂ (Pᵣ - Performance): <input type="number" step="0.01" name="b2" value="1.0"></label>
    <label>β₃ (Nᵣ - Intent): <input type="number" step="0.01" name="b3" value="1.0"></label>
    <label>β₄ (Zᵣ - Purification): <input type="number" step="0.01" name="b4" value="1.0"></label>
    <button type="button" onclick="updateZEVIEquation()">Update Equation</button>
  </form>
  <div class="formula" id="zeviEquationDisplay">Vᵣ = α + β₁·Mᵣ + β₂·Pᵣ + β₃·Nᵣ + β₄·Zᵣ + εᵣ</div>
</section>

<section>
  <h2>📝 ZEVI Survey Input</h2>
  <form id="zeviSurvey">
    <label>Mᵣ (Mission): <input type="number" step="0.01" name="m"></label>
    <label>Pᵣ (Performance): <input type="number" step="0.01" name="p"></label>
    <label>Nᵣ (Intent): <input type="number" step="0.01" name="n"></label>
    <label>Zᵣ (Purification): <input type="number" step="0.01" name="z"></label>
    <button type="button" onclick="calculateZEVI()">Calculate ZEVI</button>
  </form>
  <div class="output" id="zeviResult">ZEVI Score: —</div>
</section>

<script>
  let zeviParams = {
    alpha: 0.0,
    b1: 1.0,
    b2: 1.0,
    b3: 1.0,
    b4: 1.0
  };

  function updateZEVIEquation() {
    const form = document.forms['zeviForm'];
    zeviParams.alpha = parseFloat(form.alpha.value);
    zeviParams.b1 = parseFloat(form.b1.value);
    zeviParams.b2 = parseFloat(form.b2.value);
    zeviParams.b3 = parseFloat(form.b3.value);
    zeviParams.b4 = parseFloat(form.b4.value);

    const eq = `Vᵣ = ${zeviParams.alpha} + ${zeviParams.b1}·Mᵣ + ${zeviParams.b2}·Pᵣ + ${zeviParams.b3}·Nᵣ + ${zeviParams.b4}·Zᵣ + εᵣ`;
    document.getElementById('zeviEquationDisplay').textContent = eq;
  }

  function calculateZEVI() {
    const form = document.forms['zeviSurvey'];
    const m = parseFloat(form.m.value);
    const p = parseFloat(form.p.value);
    const n = parseFloat(form.n.value);
    const z = parseFloat(form.z.value);

    const score = zeviParams.alpha + zeviParams.b1 * m + zeviParams.b2 * p + zeviParams.b3 * n + zeviParams.b4 * z;
    document.getElementById('zeviResult').textContent = `ZEVI Score: ${score.toFixed(2)}`;
  }
</script>

<section>
  <h2>📊 Symbolic Dashboard</h2>
  <table border="1" cellpadding="10" style="width:100%; background:#fff; border-collapse: collapse;">
    <thead style="background:#2c3e50; color:white;">
      <tr>
        <th>Index</th>
        <th>Score</th>
        <th>Dimension</th>
        <th>Symbolic Classification</th>
      </tr>
    </thead>
    <tbody id="dashboardTable">
      <!-- Rows will be inserted dynamically -->
    </tbody>
  </table>
</section>

<script>
  function classify(score) {
    if (score >= 80) return "🟣 Symbolically Purified";
    if (score >= 50) return "🟡 Functionally Symbolic";
    return "⚪ Purely Material";
  }

  function updateDashboard() {
    const table = document.getElementById("dashboardTable");
    table.innerHTML = "";

    const indices = [
      { name: "ZWPI", score: parseFloat(document.getElementById("zwpiResult").textContent.split(":")[1]), dimension: "Work Performance" },
      { name: "ZLI", score: parseFloat(document.getElementById("zliResult").textContent.split(":")[1]), dimension: "Leadership" },
      { name: "ZGI", score: parseFloat(document.getElementById("zgiResult").textContent.split(":")[1]), dimension: "Governance" },
      { name: "ZII", score: parseFloat(document.getElementById("ziiResult").textContent.split(":")[1]), dimension: "Investment" },
      { name: "ZIQI", score: parseFloat(document.getElementById("ziqiResult").textContent.split(":")[1]), dimension: "Institutional Quality" },
      { name: "ZEVI", score: parseFloat(document.getElementById("zeviResult").textContent.split(":")[1]), dimension: "Existential Verification" }
    ];

    indices.forEach(index => {
      const row = document.createElement("tr");
      row.innerHTML = `
        <td><strong>${index.name}</strong></td>
        <td>${index.score.toFixed(2)}</td>
        <td>${index.dimension}</td>
        <td>${classify(index.score)}</td>
      `;
      table.appendChild(row);
    });
  }
</script>
<section>
  <h2>🌀 Symbolic Radar Chart</h2>
  <canvas id="symbolicRadar" width="400" height="400"></canvas>
  <button onclick="renderRadar()">Render Radar</button>
</section>

<script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
<script>
  function renderRadar() {
    const ctx = document.getElementById('symbolicRadar').getContext('2d');
    const data = {
      labels: ["ZWPI", "ZLI", "ZGI", "ZII", "ZIQI", "ZEVI"],
      datasets: [{
        label: "Symbolic Indices",
        data: [
          parseFloat(document.getElementById("zwpiResult").textContent.split(":")[1]),
          parseFloat(document.getElementById("zliResult").textContent.split(":")[1]),
          parseFloat(document.getElementById("zgiResult").textContent.split(":")[1]),
          parseFloat(document.getElementById("ziiResult").textContent.split(":")[1]),
          parseFloat(document.getElementById("ziqiResult").textContent.split(":")[1]),
          parseFloat(document.getElementById("zeviResult").textContent.split(":")[1])
        ],
        backgroundColor: "rgba(155, 89, 182, 0.2)", // رمزي تزكوي
        borderColor: "#8e44ad",
        pointBackgroundColor: "#f1c40f" // رمزي رسالي
      }]
    };

    new Chart(ctx, {
      type: 'radar',
      data: data,
      options: {
        scales: {
          r: {
            beginAtZero: true,
            max: 100
          }
        },
        plugins: {
          legend: {
            display: true,
            labels: {
              color: "#2c3e50"
            }
          }
        }
      }
    });
  }
</script>
<section>
  <h2>🎯 Symbolic Development Recommendations</h2>
  <button onclick="generateRecommendations()">Generate Recommendations</button>
  <div class="output" id="recommendationBox">Recommendations will appear here...</div>
</section>

<script>
  function generateRecommendations() {
    const zwpi = parseFloat(document.getElementById("zwpiResult").textContent.split(":")[1]);
    const zli = parseFloat(document.getElementById("zliResult").textContent.split(":")[1]);
    const zgi = parseFloat(document.getElementById("zgiResult").textContent.split(":")[1]);
    const zii = parseFloat(document.getElementById("ziiResult").textContent.split(":")[1]);
    const ziqi = parseFloat(document.getElementById("ziqiResult").textContent.split(":")[1]);
    const zevi = parseFloat(document.getElementById("zeviResult").textContent.split(":")[1]);

    let output = "";

    // تزكية
    const tazkiyah = (ziqi + zevi) / 2;
    if (tazkiyah < 50) {
      output += "🔮 تزكية منخفضة: نفّذ ورش تزكية جماعية، راجع النية المؤسسية، درّب القادة على التأمل الرمزي.<br>";
    }

    // رسالة
    const message = (zwpi + zii + zli) / 3;
    if (message < 50) {
      output += "📜 رسالة ضعيفة: أعد صياغة الرسالة، صمّم استثمارات رسالية، وادمج القيم في العمليات.<br>";
    }

    // انسجام
    const harmony = (zgi + zwpi) / 2;
    if (harmony < 50) {
      output += "🌀 انسجام متذبذب: حلّل الخطاب الداخلي، عزّز الانتماء، وابنِ خرائط رمزية للفرق.<br>";
    }

    if (output === "") {
      output = "✅ المؤشرات الرمزية متزكية، لا توجد توصيات حرجة حالياً.";
    }

    document.getElementById("recommendationBox").innerHTML = output;
  }
</script>
<section>
  <h2>🌐 Symbolic Visual Interface</h2>
  <svg width="400" height="400" viewBox="0 0 400 400">
    <!-- النية المركزية -->
    <circle cx="200" cy="200" r="30" fill="#34495e" />
    <text x="200" y="205" text-anchor="middle" fill="white" font-size="12">Intent</text>

    <!-- تزكية -->
    <circle cx="100" cy="100" r="40" fill="rgba(155,89,182,0.5)" />
    <text x="100" y="105" text-anchor="middle" fill="#8e44ad" font-size="14">✨ Zᵣ</text>

    <!-- الرسالة -->
    <circle cx="300" cy="100" r="40" fill="rgba(241,196,15,0.5)" />
    <text x="300" y="105" text-anchor="middle" fill="#f39c12" font-size="14">📜 Mᵣ</text>

    <!-- الانسجام -->
    <circle cx="200" cy="300" r="40" fill="rgba(52,152,219,0.5)" />
    <text x="200" y="305" text-anchor="middle" fill="#2980b9" font-size="14">🌀 Nᵣ</text>

    <!-- خطوط رمزية -->
    <line x1="200" y1="200" x2="100" y2="100" stroke="#8e44ad" stroke-width="2" />
    <line x1="200" y1="200" x2="300" y2="100" stroke="#f39c12" stroke-width="2" />
    <line x1="200" y1="200" x2="200" y2="300" stroke="#2980b9" stroke-width="2" />
  </svg>
</section>
<section>
  <h2>🎨 Customize Symbolic Identity</h2>
  <form id="identityForm">
    <label>Institution Name: <input type="text" name="name" value="Zakyah Institute"></label><br><br>
    <label>Color for Purification (Zᵣ): <input type="color" name="colorZ" value="#9b59b6"></label>
    <label>Color for Mission (Mᵣ): <input type="color" name="colorM" value="#f1c40f"></label>
    <label>Color for Harmony (Nᵣ): <input type="color" name="colorN" value="#3498db"></label><br><br>
    <label>Symbol for Purification: <input type="text" name="symbolZ" value="✨"></label>
    <label>Symbol for Mission: <input type="text" name="symbolM" value="📜"></label>
    <label>Symbol for Harmony: <input type="text" name="symbolN" value="🌀"></label><br><br>
    <button type="button" onclick="applyIdentity()">Apply Identity</button>
  </form>
  <div id="identityPreview" style="margin-top:20px;"></div>
</section>

<script>
  function applyIdentity() {
    const form = document.forms['identityForm'];
    const name = form.name.value;
    const colorZ = form.colorZ.value;
    const colorM = form.colorM.value;
    const colorN = form.colorN.value;
    const symbolZ = form.symbolZ.value;
    const symbolM = form.symbolM.value;
    const symbolN = form.symbolN.value;

    const preview = `
      <h3 style="color:#2c3e50;">Institution: ${name}</h3>
      <p><span style="color:${colorZ}; font-size:20px;">${symbolZ}</span> Purification</p>
      <p><span style="color:${colorM}; font-size:20px;">${symbolM}</span> Mission</p>
      <p><span style="color:${colorN}; font-size:20px;">${symbolN}</span> Harmony</p>
    `;
    document.getElementById('identityPreview').innerHTML = preview;
  }
</script>

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Zakyah Symbolic Platform</title>
  <style>
    body { font-family: 'Segoe UI', sans-serif; background: #f4f6fb; margin: 0; padding: 20px; }
    header { background: #2c3e50; color: white; padding: 20px; text-align: center; }
    section { background: white; margin: 20px 0; padding: 20px; border-radius: 8px; box-shadow: 0 2px 6px rgba(0,0,0,0.1); }
    h2 { color: #34495e; }
    .formula, .output { font-family: 'Courier New', monospace; background: #ecf0f1; padding: 10px; border-radius: 5px; margin-top: 10px; }
    label { display: block; margin: 8px 0; }
    input[type="number"], input[type="text"], input[type="color"] { width: 120px; margin-left: 10px; }
    button { margin-top: 10px; padding: 8px 16px; background: #2980b9; color: white; border: none; border-radius: 4px; cursor: pointer; }
    button:hover { background: #3498db; }
    table { width: 100%; border-collapse: collapse; margin-top: 10px; }
    th, td { border: 1px solid #ccc; padding: 8px; text-align: center; }
    th { background-color: #2c3e50; color: white; }
  </style>
</head>
<body>

<header>
  <h1>Zakyah Symbolic Platform</h1>
  <p>Institutional Evaluation through Purification, Mission, and Harmony</p>
</header>

<!-- تخصيص الهوية الرمزية -->
<section>
  <h2>🎨 Customize Symbolic Identity</h2>
  <form id="identityForm">
    <label>Institution Name: <input type="text" name="name" value="Zakyah Institute"></label>
    <label>Color for Purification (Zᵣ): <input type="color" name="colorZ" value="#9b59b6"></label>
    <label>Color for Mission (Mᵣ): <input type="color" name="colorM" value="#f1c40f"></label>
    <label>Color for Harmony (Nᵣ): <input type="color" name="colorN" value="#3498db"></label>
    <label>Symbol for Purification: <input type="text" name="symbolZ" value="✨"></label>
    <label>Symbol for Mission: <input type="text" name="symbolM" value="📜"></label>
    <label>Symbol for Harmony: <input type="text" name="symbolN" value="🌀"></label>
    <button type="button" onclick="applyIdentity()">Apply Identity</button>
  </form>
  <div id="identityPreview"></div>
</section>

<!-- لوحة التحكم الرمزية -->
<section>
  <h2>📊 Symbolic Dashboard</h2>
  <table>
    <thead>
      <tr>
        <th>Index</th>
        <th>Score</th>
        <th>Dimension</th>
        <th>Symbolic Classification</th>
      </tr>
    </thead>
    <tbody id="dashboardTable">
      <tr><td>ZWPI</td><td>72</td><td>Work Performance</td><td>🟡 Functionally Symbolic</td></tr>
      <tr><td>ZLI</td><td>85</td><td>Leadership</td><td>🟣 Symbolically Purified</td></tr>
      <tr><td>ZGI</td><td>60</td><td>Governance</td><td>🟡 Functionally Symbolic</td></tr>
      <tr><td>ZII</td><td>48</td><td>Investment</td><td>⚪ Purely Material</td></tr>
      <tr><td>ZIQI</td><td>78</td><td>Institutional Quality</td><td>🟡 Functionally Symbolic</td></tr>
      <tr><td>ZEVI</td><td>90</td><td>Existential Verification</td><td>🟣 Symbolically Purified</td></tr>
    </tbody>
  </table>
</section>

<!-- الرادار الرمزي -->
<section>
  <h2>🌀 Symbolic Radar Chart</h2>
  <canvas id="symbolicRadar" width="400" height="400"></canvas>
  <button onclick="renderRadar()">Render Radar</button>
</section>

<!-- التوصيات التطويرية -->
<section>
  <h2>🎯 Development Recommendations</h2>
  <button onclick="generateRecommendations()">Generate Recommendations</button>
  <div class="output" id="recommendationBox">Recommendations will appear here...</div>
</section>

<script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
<script>
  function applyIdentity() {
    const form = document.forms['identityForm'];
    const name = form.name.value;
    const colorZ = form.colorZ.value;
    const colorM = form.colorM.value;
    const colorN = form.colorN.value;
    const symbolZ = form.symbolZ.value;
    const symbolM = form.symbolM.value;
    const symbolN = form.symbolN.value;

    const preview = `
      <h3>${name}</h3>
      <p><span style="color:${colorZ}; font-size:20px;">${symbolZ}</span> Purification</p>
      <p><span style="color:${colorM}; font-size:20px;">${symbolM}</span> Mission</p>
      <p><span style="color:${colorN}; font-size:20px;">${symbolN}</span> Harmony</p>
    `;
    document.getElementById('identityPreview').innerHTML = preview;
  }

  function renderRadar() {
    const ctx = document.getElementById('symbolicRadar').getContext('2d');
    new Chart(ctx, {
      type: 'radar',
      data: {
        labels: ["ZWPI", "ZLI", "ZGI", "ZII", "ZIQI", "ZEVI"],
        datasets: [{
          label: "Symbolic Indices",
          data: [72, 85, 60, 48, 78, 90],
          backgroundColor: "rgba(155, 89, 182, 0.2)",
          borderColor: "#8e44ad",
          pointBackgroundColor: "#f1c40f"
        }]
      },
      options: {
        scales: { r: { beginAtZero: true, max: 100 } },
        plugins: { legend: { labels: { color: "#2c3e50" } } }
      }
    });
  }

  function generateRecommendations() {
    let output = "";
    const tazkiyah = (78 + 90) / 2;
    const message = (72 + 48 + 85) / 3;
    const harmony = (60 + 72) / 2;

    if (tazkiyah < 50) output += "🔮 تزكية منخفضة: نفّذ ورش تزكية جماعية، راجع النية المؤسسية.<br>";
    if (message < 50) output += "📜 رسالة ضعيفة: أعد صياغة الرسالة، صمّم استثمارات رسالية.<br>";
    if (harmony < 50) output += "🌀 انسجام متذبذب: حلّل الخطاب الداخلي، عزّز الانتماء.<br>";
    if (output === "") output = "✅ المؤشرات الرمزية متزكية، لا توجد توصيات حرجة حالياً.";

    document.getElementById("recommendationBox").innerHTML = output;
  }
</script>

</body>
</html>
<header>
  <h1>Zakyah Symbolic Platform</h1>
  <p>Institutional Evaluation through Purification, Mission, and Harmony</p>
  <h3 style="color:#8e44ad;">Founded by Prof Dr. Saleh Orabi — Creator of the Spiritual Economy Theory</h3>
</header>